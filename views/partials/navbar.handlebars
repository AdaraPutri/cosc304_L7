<nav>
  <ul>
    {{#each navItems}}
      <li class="{{#if active}}active{{/if}}">
        <a href="{{link}}">{{name}}</a>
      </li>
    {{/each}}
    <li>
        {{signedinuser}}
    </li>
  </ul>
